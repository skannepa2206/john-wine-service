services:
  - type: web
    name: john-wine-service
    env: python
    runtime: python-3.10           # ← Add this line
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: PORT
        value: 10000
